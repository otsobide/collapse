[package]
name = "collapse-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "collapse"
path = "src/main.rs"

[lib]
name = "collapse_cli"
path = "src/lib.rs"

[dependencies]
collapse-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
thiserror = "2"
