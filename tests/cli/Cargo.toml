[package]
name = "collapse-cli-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
collapse-cli = { path = "../../apps/cli" }
collapse-core = { path = "../../apps/core" }
clap = { version = "4", features = ["derive"] }
tempfile = "3"
